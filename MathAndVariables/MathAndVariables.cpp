/*
Defines a function called "add", which takes an integer called "a", and an integer called "b"
The function adds two integers together, and returns the result
*/
int add(int a, int b)
{
    // Adds the values of a and b, then returns them
    return a + b;
}

// Exercise 1 - Write a subtract function below, which takes two integers and returns the result


int main()
{
    // Defines a variable called "c", and stores the result of calling add(1, 2)
    int c = add(1, 2);
    // Exercise 2 - Create a variable "d" below, which stores the result of calling subtract(c, 1)
    // Note that by saying "c", we are using the value stored in the variable c, which is 3


    return 0;
}