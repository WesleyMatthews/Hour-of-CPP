// Tells the computer that we need the "iostream" library, which deals with input and output
#include <iostream>

// Creates the main function to be run, and returns an integer
int main()
{
    // Prints "Hello World!" and a new line to the console
    std::cout << "Hello World!\n";

    // Exits the function with a value of 0
    return 0;
}